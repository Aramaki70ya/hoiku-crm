# ===========================================
# Supabase 環境変数（Hoiku CRM）
# ===========================================
# 1. このファイルをコピー: cp .env.example .env.local
# 2. 下記の値を Supabase ダッシュボードから取得して設定
#    https://supabase.com/dashboard → プロジェクト → Settings → API
# ===========================================

# 【必須】Supabase プロジェクトの URL
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co

# 【必須】Supabase の anon public key（長い JWT 文字列）
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.xxxxx

# 【推奨】パスワードリセットメールのリンク先（未設定だと Supabase の Site URL が使われ、localhost になりがち）
# 本番: https://あなたのドメイン.vercel.app など
# NEXT_PUBLIC_APP_URL=https://your-app.vercel.app

# 【任意】デモモード（Supabase 不要でモックデータ使用）
# DEMO_MODE=true にすると認証なしで動作。本番では未設定 or false にすること。
# DEMO_MODE=true

# -------------------------------------------
# スプレッドシート取り込み（求職者管理 - 連絡先一覧）
# -------------------------------------------
# 手動「スプレッドシートから取り込み」ボタン用
# GOOGLE_SPREADSHEET_ID=スプレッドシートURLの /d/XXXXX/ の XXXXX
# GOOGLE_SHEET_NAME=連絡先一覧
# または GOOGLE_SHEET_RANGE=連絡先一覧!A1:W2000
# GOOGLE_SERVICE_ACCOUNT_KEY={"type":"service_account","client_email":"...","private_key":"..."}  （JSON文字列）

# 外部API（Google Apps Script 等から POST /api/sync/candidates）用
# SYNC_API_KEY=任意の秘密文字列
# 使用時: Authorization: Bearer <SYNC_API_KEY>
# ※ APIキー連携時は SUPABASE_SERVICE_ROLE_KEY も必要（Supabase Settings → API → service_role key）
# SUPABASE_SERVICE_ROLE_KEY=eyJhbGci...
